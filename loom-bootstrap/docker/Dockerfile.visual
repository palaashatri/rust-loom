# Loom visual-QA image: the CI image already contains Xvfb, image comparison,
# and the software-rendering dependencies needed by visual-qa-all.sh. Keep
# this layer network-free so visual QA does not depend on a second apt index
# or optional diagnostic packages.
FROM loom-bootstrap-ci

WORKDIR /workspace/loom-bootstrap

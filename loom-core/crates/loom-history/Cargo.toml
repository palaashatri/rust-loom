[package]
name = "loom-history"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Transactional undo/redo history framework"

[dependencies]
loom-command = { path = "../loom-command" }

[dev-dependencies]
[package]
name = "loom-production"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Crash recovery, localization, profiling, accessibility, and release validation for Loom applications."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"

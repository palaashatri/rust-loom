[package]
name = "loom-photo-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[[bin]]
name = "loom-photo-cli"
path = "src/main.rs"

[dependencies]
loom-photo-core = { path = "../loom-photo-core" }
